<%= form_for(current_user.gameownerships.find_by(game_id: @game.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "UnClaim", class: "btn btn-large" %>
<% end %>